!!!
%html
  %head
    %title radio.awolf.ru
    = stylesheet_link_tag 'application', :media => 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    - if params[:controller] == 'sessions'
      = yield
    - else
      .wrapper
        = render 'layouts/nav'
        .container
          = yield
        .footer-push
      = render 'layouts/footer'